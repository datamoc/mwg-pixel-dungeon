import type { DungeonRegionProfile } from './regionTypes';

export const CITY_REGION: DungeonRegionProfile = {
	id: 'city', waterSprite: 'water3', loadingSprite: 'loadingCity', wallDecoration: 'smoke', waterEmbers: false,
};
