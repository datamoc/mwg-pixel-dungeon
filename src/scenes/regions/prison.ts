import type { DungeonRegionProfile } from './regionTypes';

export const PRISON_REGION: DungeonRegionProfile = {
	id: 'prison', waterSprite: 'water1', loadingSprite: 'loadingPrison', wallDecoration: 'torch', waterEmbers: false,
};
